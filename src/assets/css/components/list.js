const setStyle = (theme) => ({
  root: {
    padding: 0
  },
  container: {
    maxHeight: 'calc(100v - 150px)',
    padding: 0
  },
  table: {
    margin: 0,
    padding: 0,
  },
})

export default { setStyle }
